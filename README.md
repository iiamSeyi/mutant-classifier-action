# Equivalent Mutant Classifier — GitHub Action

A GitHub Action that runs [PIT](https://pitest.org/) mutation testing on Java/Maven
projects and uses ML classification to automatically filter out equivalent mutants,
producing an **adjusted mutation score** that is more accurate than the raw PIT score.

Based on: *"Enhancing Mutation Testing Efficiency: A Comparative Study of Machine
Learning Models for Equivalent Mutant Identification"* — Pugh et al., Towson University, 2025.

---

## Repository structure

```
mutant-classifier-action/
├── action.yml                          # GitHub Action metadata + inputs/outputs
├── Dockerfile                          # Container: Java 17 + Maven + Python 3.11
├── entrypoint.sh                       # Orchestrates PIT → classifier → report
├── classify.py                         # Classifier (Phase 1 stub, ML in Phase 3)
├── requirements.txt                    # Python dependencies
├── sample-java-project/                # Test target — the action runs against this
│   ├── pom.xml                         # Maven build with PIT plugin configured
│   └── src/
│       ├── main/java/com/example/Calculator.java
│       └── test/java/com/example/CalculatorTest.java
└── .github/workflows/
    └── mutation-ci.yml                 # Runs the action against sample-java-project
```

The workflow uses `uses: ./` to reference the action defined in this same repo —
the standard pattern for testing a GitHub Action during development.

---

## How to use this action in another project

Once published, add this to the target project's `.github/workflows/mutation-ci.yml`:

```yaml
name: Mutation Testing

on:
  pull_request:
    branches: [ main ]
    paths: [ 'src/**/*.java', 'pom.xml' ]

permissions:
  contents: read
  pull-requests: write

jobs:
  mutation-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run mutation testing
        id: classifier
        uses: your-org/mutant-classifier-action@v1
        with:
          confidence-threshold: '0.90'
          fail-below-score: '0.70'

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mutation-report
          path: target/mutant-classifier-report/
```

---

## Inputs

| Input | Description | Default |
|-------|-------------|---------|
| `working-directory` | Path to Maven project root (where pom.xml lives) | `.` |
| `confidence-threshold` | Min ML confidence to classify a mutant as equivalent | `0.90` |
| `fail-below-score` | Fail workflow if adjusted score drops below this | `0.0` |
| `maven-args` | Extra args passed to `mvn pitest:mutationCoverage` | `''` |
| `upload-report` | Upload HTML+JSON report as artifact | `true` |

## Outputs

| Output | Description |
|--------|-------------|
| `raw-mutation-score` | PIT score before filtering (0.0–1.0) |
| `adjusted-mutation-score` | Score after equivalent mutants removed (0.0–1.0) |
| `equivalent-mutants-found` | Count of mutants classified as equivalent |
| `total-mutants` | Total mutants generated by PIT |

---

## Development phases

- **Phase 1** ✅ CI plumbing — PIT integration, XML parsing, reporting, GitHub outputs
- **Phase 2** — Java AST feature extraction and Word2Vec embedding pipeline
- **Phase 3** — ML model training on Java mutants and live inference
